# This file is a stub for the native Rust extension.
# It gets replaced by the compiled extension during `maturin develop` or `maturin build`.
#
# If you see this message at runtime, the native extension wasn't built.
# Run: maturin develop
raise ImportError(
    "tenuo_core native extension not built. "
    "Run 'maturin develop' in the tenuo-python directory."
)
