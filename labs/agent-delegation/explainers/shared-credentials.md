# Shared credentials and blast radius

One key for every agent means you cannot restrict one agent without restricting all of them. Whatever the least careful agent can be talked into, every agent can do. The wallet in stage 1 is the blast radius made visible.
