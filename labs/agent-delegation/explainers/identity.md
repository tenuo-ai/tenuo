# Identity-based access control

Cloud IAM, service accounts, and role-based access all attach permissions to *who* is acting. That stops an agent from doing another role's job. It cannot stop an agent from doing its own job on the wrong thing, because the role says *reservations* and AA882 is a reservation.
