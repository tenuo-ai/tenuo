# Privilege escalation

Ending up with more access than you were given. In a policy-service model, whether Check-in Agent can arrange broader access for Boarding Agent depends on a separate permission on the write endpoint, which most deployments get wrong, because it is about who may write policy and not about what the writer holds.
