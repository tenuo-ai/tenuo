# Scoped rules

A rule that names the resource, the destination, and the ceiling is a real improvement, and stage 3 shows it working. It works because there is one task. The rule is still about the agent, so the moment the agent does two jobs at once, the rule has to cover both.
