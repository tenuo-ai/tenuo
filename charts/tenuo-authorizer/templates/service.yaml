apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenuo-authorizer.fullname" . }}
  labels:
    {{- include "tenuo-authorizer.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "tenuo-authorizer.selectorLabels" . | nindent 4 }}
